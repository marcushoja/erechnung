server:
  port: ${PORT:8080}

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  basic:
    enabled: ${APP_BASIC_ENABLED:false}
    username: ${APP_BASIC_USER:freelancer}
    password: ${APP_BASIC_PASS:freelancer2026}
